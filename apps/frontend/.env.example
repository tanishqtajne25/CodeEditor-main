# Copy this file to .env and fill in the values before building for production.
#
# For direct EC2 access (no TLS), use:
# VITE_API_URL=http://65.2.170.124:3000
# VITE_WS_URL=ws://65.2.170.124:5000
#
# If frontend is served over HTTPS (for example on Vercel), prefer HTTPS/WSS
# backend endpoints to avoid browser mixed-content blocking.

VITE_API_URL=
VITE_WS_URL=
